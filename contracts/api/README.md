# API-Verträge (geplant)

Maschinenlesbare Spezifikation der Digiwiz Agent- und Runtime-API:

- `POST /api/v1/agenten/validate`, `/lieferung`
- `GET /api/v1/playbooks/*`
- `POST /api/v1/runtime/task`

**Status:** Verfahren in `docs/verfahren/` — OpenAPI-Artefakte folgen inkrementell (ADR-0007).

Implementierung bleibt in `digiwiki/11_wiki_api.py`; dieser Ordner wird die **kanonische Contract-Quelle**.
